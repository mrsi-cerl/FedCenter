---
const { cardTitle, buttonTitle = "More", media, size, link } = Astro.props;
// media without extension
const filename = media ? media.replace(/\.[^/.]+$/, "") : null; //not all cards have media associated with them
---

<li class=`usa-card tablet:grid-col-${size}`>
  <div class="usa-card__container">
    <div class="usa-card__header">
      <h4 class="usa-card__heading">{cardTitle}</h4>
    </div>
    {
      media && (
        <div class="usa-card__media">
          <div class="usa-card__img maxh-card-lg">
            <picture>
              <source srcset={`${filename}.avif`} type="image/avif" />
              <source srcset={`${filename}.webp`} type="image/webp" />
              <img src={media} alt="Card Image" width="800" height="600" />
            </picture>
          </div>
        </div>
      )
    }
    <div class="usa-card__body">
      <slot />
    </div>
    <div class="usa-card__footer">
      <a href={link} class="usa-button">{buttonTitle}</a>
    </div>
  </div>
</li>
